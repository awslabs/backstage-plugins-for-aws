# Backstage plugins for AWS - Sample AWS resources

This CDK project provides sample AWS resources that can be used to test the plugins in the main project.

Run:

```bash
yarn install

cdk deploy
```
